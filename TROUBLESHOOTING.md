# Диагностика проблемы с чат-ботом на Render.com

## Проблема
Чат-бот работает локально, но не работает на сервере (Render.com).

## Шаг 1: Проверьте URL сервера

В файле `demo/index.html` на строке 320 указан:
```javascript
const PRODUCTION_API_URL = 'https://agrocredit-server.onrender.com';
```

**Действия:**
1. Откройте Render.com dashboard
2. Найдите ваш Flask сервер
3. Скопируйте точный URL (например: `https://agrocredit-server-abc123.onrender.com`)
4. Замените URL в `demo/index.html` на строке 320

## Шаг 2: Проверьте, что сервер запущен

Откройте в браузере:
```
https://ваш-сервер.onrender.com/chat
```

**Ожидаемый результат:**
- Должна быть ошибка 405 (Method Not Allowed) - это нормально, значит сервер работает
- Если 404 или timeout - сервер не запущен

## Шаг 3: Проверьте CORS настройки

В Render.com dashboard для вашего сервера:

1. Перейдите в **Environment**
2. Проверьте переменную `ALLOWED_ORIGINS`
3. Она должна содержать URL вашей демо-страницы

**Пример:**
```
ALLOWED_ORIGINS=https://ваша-демо.onrender.com,http://localhost:3000
```

## Шаг 4: Проверьте переменные окружения

Убедитесь, что на Render.com установлены:

| Переменная | Значение |
|------------|----------|
| `GOOGLE_API_KEY` | Ваш Gemini API ключ |
| `ALLOWED_ORIGINS` | URL демо-страницы |
| `PORT` | 3000 (опционально) |

## Шаг 5: Проверьте логи сервера

1. Откройте Render.com dashboard
2. Перейдите в ваш сервер → **Logs**
3. Ищите ошибки CORS или другие проблемы

## Шаг 6: Тест с curl

Проверьте сервер напрямую:

```bash
curl -X POST https://ваш-сервер.onrender.com/chat \
  -H "Content-Type: application/json" \
  -H "Origin: https://ваша-демо.onrender.com" \
  -d '{"message": "test"}'
```

**Ожидаемый ответ:**
```json
{"reply": "..."}
```

## Шаг 7: Проверьте браузер Console

1. Откройте демо-страницу на Render.com
2. Нажмите F12 (Developer Tools)
3. Перейдите в **Console**
4. Найдите сообщение: `🌐 API Base URL: ...`
5. Убедитесь, что URL правильный

## Частые проблемы и решения

### Проблема: CORS error
```
Access-Control-Allow-Origin header is missing
```

**Решение:**
Добавьте URL демо-страницы в `ALLOWED_ORIGINS` на сервере.

### Проблема: 500 Internal Server Error

**Решение:**
Проверьте `GOOGLE_API_KEY` в переменных окружения.

### Проблема: Timeout / Connection refused

**Решение:**
- Сервер спит (Render free tier) - подождите 30 секунд
- Сервер не запущен - проверьте логи деплоя

### Проблема: 404 Not Found

**Решение:**
Проверьте, что в `app.py` есть маршрут `/chat` и сервер запущен.

## Быстрая проверка

**Вопрос 1:** Какой URL вашего сервера на Render.com?
- Найдите в Render dashboard

**Вопрос 2:** Какой URL вашей демо-страницы?
- Найдите в Render dashboard или где вы разместили демо

**Вопрос 3:** Совпадают ли эти URL с настройками в коде?
- `PRODUCTION_API_URL` в `index.html` (строка 320)
- `ALLOWED_ORIGINS` в Render environment variables

## Что делать дальше

Пожалуйста, предоставьте:
1. ✅ URL вашего сервера на Render.com
2. ✅ URL вашей демо-страницы
3. ✅ Скриншот ошибки из браузера Console (F12)
4. ✅ Логи с Render.com (если есть ошибки)

После этого я смогу точно определить проблему и исправить её!
